# HY-MT 翻译服务配置

# 服务端口
PORT=8021

# 模型名称 (HuggingFace)
MODEL_NAME=tencent/HY-MT1.5-1.8B

# 本地模型缓存路径
MODEL_PATH=./models

# GPU 空闲超时（秒），超时后自动释放显存
GPU_IDLE_TIMEOUT=300

# GPU ID（留空则自动选择最空闲的GPU）
NVIDIA_VISIBLE_DEVICES=

# HuggingFace 镜像（国内加速）
HF_ENDPOINT=https://hf-mirror.com
